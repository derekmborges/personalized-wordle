generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    // url = env("DATABASE_URL")
    url = "file:./dev/db"
}

model PersonalizedWordle {
    id       String @unique
    word     String
    message  String?
    creatorName String
}
